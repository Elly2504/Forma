---
// Direct conversion CTA - links to pricing section
// Phase 1: Added social proof counter and trust badges
---

<section class="relative min-h-screen flex items-center pt-20 pb-16 overflow-hidden">
  <!-- Background effects -->
  <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[600px] bg-[var(--gold-500)] rounded-full blur-[200px] opacity-10"></div>
  <div class="absolute bottom-0 right-0 w-[400px] h-[400px] bg-[var(--gold-600)] rounded-full blur-[150px] opacity-5"></div>
  
  <div class="container relative">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Social Proof Counter - Animated -->
      <div class="flex items-center justify-center gap-3 mb-6 animate-fade-in">
        <div class="flex items-center gap-2 bg-green-500/10 border border-green-500/30 rounded-full px-4 py-2">
          <span class="relative flex h-3 w-3">
            <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
            <span class="relative inline-flex rounded-full h-3 w-3 bg-green-500"></span>
          </span>
          <span class="text-green-400 font-semibold">
            <span id="verified-count" class="tabular-nums">45,000</span>+ Product Codes Verified
          </span>
        </div>
      </div>

      <!-- Badge -->
      <div class="counter-badge mx-auto mb-8 animate-fade-in" style="animation-delay: 0.05s">
        <span class="counter-dot"></span>
        <span>The Authentication API for Vintage Retailers</span>
      </div>
      
      <!-- Main headline -->
      <h1 class="text-3xl sm:text-5xl lg:text-7xl font-extrabold leading-tight mb-6 animate-fade-in" style="animation-delay: 0.1s">
        Instant Verification <br/>
        <span class="text-gradient">For Your Platform</span>
      </h1>
      
      <!-- Subheadline -->
      <p class="text-lg sm:text-xl lg:text-2xl text-[var(--text-secondary)] max-w-2xl mx-auto mb-8 animate-fade-in" style="animation-delay: 0.2s">
        Integrate AI-powered authentication into your marketplace. 
        <strong class="text-white">One API call</strong> = verified vintage football shirts.
      </p>

      <!-- Trust Badges - KitTicker Advantages -->
      <div class="flex flex-wrap items-center justify-center gap-3 mb-10 animate-fade-in" style="animation-delay: 0.25s">
        <div class="flex items-center gap-2 bg-zinc-800/50 border border-zinc-700 rounded-full px-4 py-2 text-sm">
          <svg class="w-4 h-4 text-amber-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
          </svg>
          <span class="text-zinc-300">Data-First</span>
        </div>
        <div class="flex items-center gap-2 bg-zinc-800/50 border border-zinc-700 rounded-full px-4 py-2 text-sm">
          <svg class="w-4 h-4 text-amber-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
          </svg>
          <span class="text-zinc-300">Explainable AI</span>
        </div>
        <div class="flex items-center gap-2 bg-zinc-800/50 border border-zinc-700 rounded-full px-4 py-2 text-sm">
          <svg class="w-4 h-4 text-amber-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
          </svg>
          <span class="text-zinc-300">API-Ready</span>
        </div>
        <div class="flex items-center gap-2 bg-zinc-800/50 border border-zinc-700 rounded-full px-4 py-2 text-sm">
          <svg class="w-4 h-4 text-amber-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z" />
          </svg>
          <span class="text-zinc-300">DPP Ready</span>
        </div>
      </div>
      
      <!-- CTA - B2B focused -->
      <div class="animate-fade-in flex flex-col sm:flex-row items-center justify-center gap-4" style="animation-delay: 0.3s">
        <a href="/pricing" class="btn-primary text-lg px-8 py-4">
          Get API Key
        </a>
        <a href="/docs" class="text-amber-400 hover:text-amber-300 flex items-center gap-2">
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" />
          </svg>
          View API Docs
        </a>
      </div>
      
      <!-- Social proof - B2B focused -->
      <div class="mt-12 animate-fade-in" style="animation-delay: 0.4s">
        <p class="text-[var(--text-muted)] text-sm mb-4">Built for platforms selling on</p>
        <div class="flex items-center justify-center gap-6 sm:gap-8 opacity-60 flex-wrap">
          <span class="text-lg sm:text-xl font-semibold">Depop</span>
          <span class="text-lg sm:text-xl font-semibold">Vinted</span>
          <span class="text-lg sm:text-xl font-semibold">eBay</span>
          <span class="text-lg sm:text-xl font-semibold">Classic Football Shirts</span>
        </div>
      </div>
    </div>
    
    <!-- Hero visual mockup -->
    <div class="mt-16 max-w-5xl mx-auto animate-fade-in gold-glow rounded-2xl" style="animation-delay: 0.5s">
      <div class="glass-card-gold p-4 md:p-8">
        <!-- Mock dashboard preview -->
        <div class="bg-[var(--bg-primary)] rounded-xl overflow-hidden">
          <div class="flex items-center gap-2 px-4 py-3 bg-[var(--bg-elevated)] border-b border-[var(--border-subtle)]">
            <div class="w-3 h-3 rounded-full bg-red-500"></div>
            <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
            <div class="w-3 h-3 rounded-full bg-green-500"></div>
            <span class="ml-4 text-xs text-[var(--text-muted)]">app.kitticker.com</span>
          </div>
          <div class="p-6 md:p-10">
            <div class="grid md:grid-cols-2 gap-8">
              <!-- Shirt preview side -->
              <div class="space-y-4">
                <div class="aspect-square bg-[var(--bg-card)] rounded-xl flex items-center justify-center">
                  <div class="text-center">
                    <div class="text-6xl mb-2">👕</div>
                    <p class="text-[var(--text-muted)] text-sm">Arsenal 1991 Away</p>
                    <p class="text-[var(--text-muted)] text-xs">JVC Sponsor - Adidas</p>
                  </div>
                </div>
                <div class="grid grid-cols-4 gap-2">
                  {['Front', 'Back', 'Label', 'Detail'].map((label) => (
                    <div class="aspect-square bg-[var(--bg-card)] rounded-lg flex items-center justify-center cursor-pointer hover:bg-[var(--bg-elevated)] transition-colors">
                      <span class="text-[var(--text-muted)] text-xs">{label}</span>
                    </div>
                  ))}
                </div>
              </div>
              
              <!-- Valuation result side -->
              <div class="space-y-6">
                <div class="text-center md:text-left">
                  <p class="text-[var(--text-muted)] text-sm">Estimated Value</p>
                  <p class="text-5xl font-bold text-gradient">£185 - £220</p>
                  <p class="text-green-400 text-sm mt-1">+12% in last 90 days</p>
                </div>
                
                <div class="space-y-3">
                  <div class="flex justify-between items-center">
                    <span class="text-[var(--text-secondary)]">Condition Grade</span>
                    <span class="bg-[var(--gold-500)] text-black px-3 py-1 rounded-full text-sm font-bold">A-</span>
                  </div>
                  <div class="flex justify-between items-center">
                    <span class="text-[var(--text-secondary)]">Fabric Quality</span>
                    <div class="flex gap-1">
                      {[1,2,3,4,5].map((i) => (
                        <div class={`w-4 h-2 rounded-full ${i <= 4 ? 'bg-[var(--gold-500)]' : 'bg-[var(--bg-card)]'}`}></div>
                      ))}
                    </div>
                  </div>
                  <div class="flex justify-between items-center">
                    <span class="text-[var(--text-secondary)]">Print Condition</span>
                    <div class="flex gap-1">
                      {[1,2,3,4,5].map((i) => (
                        <div class={`w-4 h-2 rounded-full ${i <= 5 ? 'bg-[var(--gold-500)]' : 'bg-[var(--bg-card)]'}`}></div>
                      ))}
                    </div>
                  </div>
                  <div class="flex justify-between items-center">
                    <span class="text-[var(--text-secondary)]">Authenticity</span>
                    <span class="text-green-400 flex items-center gap-1">
                      <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                      </svg>
                      Verified
                    </span>
                  </div>
                </div>
                
                <a href="/pricing" class="btn-primary w-full block text-center">Integrate API</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Animated counter script -->
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const counter = document.getElementById('verified-count');
    if (!counter) return;
    
    const target = 45000;
    const duration = 2500;
    const startTime = performance.now();
    
    function formatNumber(num: number): string {
      return num.toLocaleString('en-US');
    }
    
    function animate(currentTime: number) {
      const elapsed = currentTime - startTime;
      const progress = Math.min(elapsed / duration, 1);
      
      // Easing function for smooth animation
      const easeOut = 1 - Math.pow(1 - progress, 3);
      const current = Math.floor(easeOut * target);
      
      counter.textContent = formatNumber(current);
      
      if (progress < 1) {
        requestAnimationFrame(animate);
      } else {
        counter.textContent = formatNumber(target);
      }
    }
    
    // Start animation when element is in viewport
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          requestAnimationFrame(animate);
          observer.disconnect();
        }
      });
    });
    
    observer.observe(counter);
  });
</script>
